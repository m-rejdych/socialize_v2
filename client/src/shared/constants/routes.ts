const ROUTES = {
  LOGIN: '/login',
  REGISTER: '/register',
  HOME: '/home',
  PROFILE: '/profile',
  CHAT: '/chat',
  POST: '/post',
  NOTIFICATIONS: '/notifications',
} as const;

export default ROUTES;
